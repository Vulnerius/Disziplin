apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-proxy-deployment
  labels:
    app: nginx-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-proxy
  template:
    metadata:
      labels:
        app: nginx-proxy
    spec:
      containers:
        - name: nginx-proxy-container
          image: ghcr.io/vulnerius/disziplin/nginx:42f2855aa1298eb6f3a503d953374ecd3ea025ad
          ports:
            - containerPort: 80
            - containerPort: 443 # Wenn du HTTPS planst
      imagePullSecrets:
        - name: ghcr-secret
