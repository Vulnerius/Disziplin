apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-proxy-deployment
  labels:
    app: nginx-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-proxy
  template:
    metadata:
      labels:
        app: nginx-proxy
    spec:
      containers:
        - name: nginx-proxy-container
          image: ghcr.io/vulnerius/disziplin/nginx:063bbabde44f149b867b9c871f88c306d0f52ebf
          ports:
            - containerPort: 80
            - containerPort: 443 # Wenn du HTTPS planst
      imagePullSecrets:
        - name: ghcr-secret
