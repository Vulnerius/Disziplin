apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-proxy-deployment
  labels:
    app: nginx-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-proxy
  template:
    metadata:
      labels:
        app: nginx-proxy
    spec:
      containers:
        - name: nginx-proxy-container
          image: ghcr.io/vulnerius/disziplin/nginx:b15198c1ee3b3e667c553354e6c64230dbbd20a0
          ports:
            - containerPort: 80
            - containerPort: 443
          volumeMounts:
            - mountPath: "/etc/nginx/certs"
              name: tls-cert
              readOnly: true
      volumes:
        - name: tls-cert
          secret:
            secretName: disziplin-tls-secret
      imagePullSecrets:
        - name: ghcr-secret
